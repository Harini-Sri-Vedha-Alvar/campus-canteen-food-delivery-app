{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "workspaces": [
    "artifacts/*",
    "lib/*",
    "scripts"
  ],
  "scripts": {
    "start": "concurrently \"npm run --workspace=@workspace/api-server dev\" \"npm run dev:frontend\"",
    "dev": "npm start",
    "dev:frontend": "set PORT=5180 && set BASE_PATH=/ && npm run --workspace=@workspace/food-delivery dev",
    "dev:mockup": "npm run --workspace=@workspace/mockup-sandbox dev",
    "build": "npm run typecheck && npm run build:all",
    "build:all": "npm run --workspaces build",
    "typecheck:libs": "tsc --build",
    "typecheck": "npm run typecheck:libs && npm run --workspaces --if-present typecheck"
  },
  "private": true,
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prettier": "^3.8.1",
    "typescript": "~5.9.2"
  }
}
